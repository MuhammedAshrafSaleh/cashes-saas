// lib/core/auth/auth_state_listener.dart
// Wired into app.dart — see AppState.initState()
// SessionGuard is initialized once and torn down when app disposes.
export 'package:cashes/core/auth/session_guard.dart';
